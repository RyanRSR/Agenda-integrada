import ContCent from "../Components/ContCent";

function AgendaPage({ setIsModalOpen, isModalOpen }) {
  return (
    <ContCent
      name="Ryan"
      setIsModalOpen={setIsModalOpen}
      isModalOpen={isModalOpen}
    />
  );
}

export default AgendaPage;
